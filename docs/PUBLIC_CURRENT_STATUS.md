# Public Current Status

Статус: публичный текущий статус Garmon World.

Последний review: 2026-05-12.

## Текущая позиция

Garmon World — публичная исследовательская поверхность.

Центральная публичная идея:

> искусственный ответ может иметь происхождение до речи.

Garmon исследует путь: internal state, body contour, meaning, choice boundary, trace, speech.

Компактная рамка:

```text
state → body contour → meaning → choice boundary → trace → speech
```

## Текущий результат

Garmon имеет static/shadow evidence layer вокруг body-contour distinction до речи LLM.

Несколько кандидатов body-contour остаются различимыми до финальной речи:

- quiet baseline;
- fatigue;
- recovery;
- stress;
- mixed stress/recovery;
- day/night rhythm.

Эти контуры не схлопываются в generic label.

Это делает часть пути до речи inspectable.

## Публично-безопасная цепочка

```text
body contour
→ meaning candidate
→ passive context
→ choice visibility boundary
→ choice ownership boundary
→ behavior boundary
```

Расширенная цепочка:

```text
body contour
→ meaning candidate
→ passive context
→ choice visibility boundary
→ choice ownership boundary
→ behavior boundary
→ pre-selected result boundary candidate (synthetic)
```

## Что это показывает

Evidence для:

- body-contour distinction до речи;
- contours к meaning candidates;
- passive context;
- choice visibility;
- choice ownership;
- behavior;
- synthetic marker на pre-selected result boundary.

Это даёт inspectable pre-speech review surface.

## Более широкое направление

```text
internal state → meaning → choice boundary → time/trace → speech
```

Короткий закон:

```text
participation is not power
```

## Следующий артефакт

Small body-contour distinction artifact, показывающий, как quiet baseline, fatigue, recovery, stress, mixed stress/recovery и day/night rhythm остаются различимыми до финальной речи.

Артефакт показывает принцип разных внутренних контуров, формирующих смысл до речи.

## One-Line Summary

Garmon имеет public-safe review surface для origin ответа до речи: distinguishable body contours к meaning и protected choice-boundary markers.